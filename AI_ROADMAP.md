# 🤖 Deno Deploy AI 边缘应用全栈演进路线图 (AI Edge Roadmap)

本项目旨在将 `deno-case` 从边缘能力展示控制台，逐步演化为一个高价值、全栈、具备生产级架构的 **AI 边缘应用与网关服务平台**。

---

## 🚩 里程碑路线图规划

```
┌─────────────────────────────────────────────────────────────────┐
│ 里程碑 1：AI 边缘 API 网关与 Token 缓存中转站 (Phase 1)           │
│ ├─ 支持 DeepSeek / OpenAI / SiliconFlow 通用 SSE 流式代理         │
│ ├─ Deno KV 大模型响应智能缓存 (节省 Token 成本)                  │
│ └─ 隐藏后端真实 Key，安全防护与客户端 Token 打字机交互           │
└────────────────────────────────┬────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ 里程碑 2：AI 文本摘要与金句排版小卡片生成器 (Phase 2)               │
│ ├─ 长文本 / 网页链接一键智能提炼核心观点                          │
│ └─ 前端高颜值 Canvas / SVG 美化卡片渲染与 PNG 一键导出下载       │
└────────────────────────────────┬────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ 里程碑 3：AI 网页剪藏插件后端与个人知识中心 (Phase 3)              │
│ ├─ 开放标准化 Webhook / REST 剪藏接口                             │
│ └─ Deno KV 持久化随手记、标签归档与历史查询                       │
└────────────────────────────────┬────────────────────────────────┘
                                 │
                                 ▼
┌─────────────────────────────────────────────────────────────────┐
│ 里程碑 4：多人实时协同 AI 讨论室与 Prompt 实验室 (Phase 4)          │
│ ├─ 基于 WebSocket + Deno KV Watch 实现多端实时协同              │
│ └─ 房间多人同步推演 Prompt，AI 响应流实时全员广播                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🛠️ 里程碑 1 详细功能设计 (当前迭代)

### 核心功能列表

1. **API 网关代理 (`POST /api/ai/chat`)**：
   - 兼容 OpenAI / DeepSeek API 格式。
   - 环境变量读取 `AI_API_KEY` 与 `AI_BASE_URL`（支持官方及第三方中转）。
   - 实现边缘原生的 `ReadableStream` 零延迟流式（SSE）透传。
2. **Deno KV 智能 Token 缓存**：
   - 根据用户的 Prompt 生成 SHA-256 哈希作为缓存键 `["ai_cache", hash]`。
   - 若命中缓存，则以极高响应速度直接吐出缓存内容，避免重复扣除大模型 Token 费用。
3. **前端 AI Playground 交互面板**：
   - 在 Dashboard 侧边栏新增 **`🤖 AI 网关与对话`** 菜单。
   - 支持自定义 Model 选择、System Prompt 设定、实时打字机输出与缓存命中状态提示。
